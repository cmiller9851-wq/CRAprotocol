[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "CRAprotocol"
version = "2.1.0"
description = "CRA Protocol Execution and Telemetry Enforcer"
dependencies = [
    "pandas>=3.0.0",
    "numpy>=1.26.0"
]
